<ul>
    <%= for item <- @items do %>
    <li>
        <%= item.id %> <strong> <%= link(item.title, to: Routes.item_path(@conn, :show, item)) %></strong> <%= item.description %>
    </li>
    <% end %>
</ul>